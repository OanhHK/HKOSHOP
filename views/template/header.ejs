<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="#">HKO-Shop</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <%configHeader.forEach(function(hitem) { %>
          <li class="nav-item <% if (hitem.name == currpage) { %>
            active <%}%> ">
            <a class="nav-link" href="<%=hitem.link%>"><%=hitem.name%></a>
          </li>
        <% }); %>  
      </ul>
      
      <form class="form-inline my-2 my-lg-0">
        <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
      </form>
 
      <ul class="navbar-nav mr-auto"></ul>
        <% if (username) { %> %>
          <li class="nav-item">
            <a class="nav-link" href="/logout"> <%=username%> - Logout</a>
          </li>          
        <% } %>
      </ul>


      <% if (currpage == "Product") { %>
        <span style="color:white;">
            <i class="fas fa-shopping-cart"></i>
            Cart 
            <span id="numberItems"></span>
        </span>
      <%}%>
    </div>
  </nav>

  <div>
    <nav id="fv">
      <ul>
        <li><b><a href="product/create">Add Product</b></a></li>
        <li><b><a href="https://hkoshop.herokuapp.com/chatting">chatting</b></a></li>
        <li>
        </nav>
      </div>
  <style>
  
  #fv{
    width: 100%;
    height: 35px;
    background-color: #272525;
    display: block;
    box-shadow: 0px 2px 2px rgba(63, 61, 61, 0.5); /*Đổ bóng cho menu*/
    position:static; /*Cho menu cố định 1 vị trí với top và left*/
    top: 0; /*Nằm trên cùng*/
    right:0; /*Nằm sát bên trái*/
    z-index: 100000; /*Hiển thị lớp trên cùng*/
  }
  #fv ul{
    margin: 0;
    padding: 0;
  }
  #fv ul li{
    list-style:none inside;
    width: auto;
    float: left;
    line-height: 35px; /*Cho text canh giữa menu*/
    color: #fff;
    padding: 0;
    margin-right:10px;
    position: relative;
  }
  #fv ul li a{
    text-transform: uppercase;
    white-space: nowrap; /*Cho chữ trong menu không bị wrap*/
    padding: 0 10px;
    color: #fff;
    display: block;
    font-size: 1em;
    text-decoration: none;
  }
  </style>
